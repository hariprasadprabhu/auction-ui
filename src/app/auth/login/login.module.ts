import { FormsModule } from '@angular/forms';
import { BrowserModule } from '@angular/platform-browser';
import { Login } from './login';

// Removed the NgModule as it is not needed for standalone components.